@AGENTS.md

All project instructions live in `AGENTS.md` (the cross-agent standard file, also read by
Codex, Gemini CLI, and other tools). The line above imports it into Claude Code automatically.
Make all edits in `AGENTS.md` — never add project guidance to this file.
